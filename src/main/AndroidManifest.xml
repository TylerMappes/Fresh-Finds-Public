<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreshFinds"
        tools:targetApi="31">

        <activity
            android:name=".freshfinds.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FreshFinds">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".freshfinds.LoginActivity"
            android:exported="true" />

        <activity
            android:name=".freshfinds.CreateAccountActivity"
            android:exported="true" />

        <activity
            android:name=".freshfinds.ChooseGenresActivity"
            android:exported="true" />

        <activity
            android:name=".freshfinds.ArtistProfileActivity"
            android:exported="true" />

        <activity
            android:name=".freshfinds.UploadPictureActivity"
            android:exported="true" />

        <activity
            android:name=".freshfinds.CreateBiographyActivity"
            android:exported="true" />

        <activity
            android:name=".freshfinds.ViewAnalyticsActivity"
            android:exported="true" />

        <!-- Add any other activities as needed -->
    </application>

</manifest>
